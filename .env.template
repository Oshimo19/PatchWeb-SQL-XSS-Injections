# =====================================================
# PATCHWEB - ENV GLOBAL (LOCAL + DOCKER) - TEMPLATE
# =====================================================
# Ce fichier est un modèle.
# Copier ce fichier en .env et remplacer les valeurs
# <BACKEND_PORT> et <FRONTEND_PORT> par des ports libres.
# =====================================================

# ---------------------
# ENV GENERAL
# ---------------------
NODE_ENV=development

# ---------------------
# BACKEND
# ---------------------
# Port exposé du backend sur la machine hôte
# Exemple : 8000, 8080, 9000, etc.
BACKEND_PORT=<BACKEND_PORT>
BACKEND_CONTAINER=secure-backend

# SQLite (chemin valide dans le conteneur backend)
DB_PATH=/app/data/database.db

# CORS
# Origine autorisée du frontend (navigateur)
# Doit correspondre exactement au port frontend exposé
FRONTEND_ORIGIN=http://localhost:<FRONTEND_PORT>

# API externe (publique)
RANDOMUSER_URL=https://randomuser.me/api/

# ---------------------
# FRONTEND
# ---------------------
# Port exposé du frontend sur la machine hôte
FRONTEND_PORT=<FRONTEND_PORT>
FRONTEND_CONTAINER=secure-frontend

# Backend exposé au frontend (navigateur)
# Toujours utiliser localhost côté navigateur
REACT_APP_API_URL=http://localhost:<BACKEND_PORT>

# ---------------------
# TESTS
# ---------------------
TESTS_CONTAINER=security-tests

# URLs internes Docker (réseau docker-compose)
# Ces valeurs ne doivent PAS utiliser localhost
# Elles sont utilisées uniquement depuis les conteneurs Docker
TEST_API_URL=http://backend:<BACKEND_PORT>
TEST_FRONT_URL=http://frontend:<FRONTEND_PORT>
